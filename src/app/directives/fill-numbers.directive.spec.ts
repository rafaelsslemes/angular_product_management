import { FillNumbersDirective } from './fill-numbers.directive';

describe('FillNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new FillNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
